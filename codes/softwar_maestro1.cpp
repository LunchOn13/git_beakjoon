#include <iostream>
#include <vector>

using namespace std;

int main()
{
    // k 개의 스킬이름?
    vector<char> arr;
    
    char k;
    while(cin >> k)
    {
        arr.push_back(k);
    }
    
    // 연계 스킬 개수 
    int n;
    cin >> n;

    // 이제 연계가 가능한 가짓수를 준다는거지?
    
}